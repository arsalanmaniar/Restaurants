# URL of the AbhiAya backend the dashboards should call.
# Locally: http://localhost:8020 (matches the fallback in src/lib/api.ts).
# Production: the HF Space / Render URL, e.g. https://arsalanmaniar-restaurants.hf.space
NEXT_PUBLIC_API_URL=
